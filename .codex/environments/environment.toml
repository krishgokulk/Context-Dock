# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "Context-Dock"

[setup]
script = '''
#!/bin/bash
set -euo pipefail

# Move to project root
cd "$WORKTREE_ROOT"

# Resolve Swift packages (SwiftTerm via SPM). Retry once on the transient
# "Couldn't fetch updates from remote repositories" cache flake.
xcodebuild -project Context-Dock.xcodeproj -resolvePackageDependencies \
  || xcodebuild -project Context-Dock.xcodeproj -resolvePackageDependencies

# Pre-create the local derived data dir the build scripts actually use.
mkdir -p .build/XcodeDerivedData

echo "Context-Dock environment ready"
'''

[[actions]]
name = "Run"
icon = "run"
command = "./script/build_and_run.sh"
platform = "darwin"

[[actions]]
name = "Build"
icon = "tool"
command = "./script/build_and_run.sh build"
platform = "darwin"

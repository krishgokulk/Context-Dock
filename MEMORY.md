# MEMORY — cross-session shift log

Append-only. One line per session, newest at the bottom. Never rewrite or delete someone
else's line — several agents write here, and appending is what keeps that safe.

Format: `YYYY-MM-DD · agent/branch · did · next · blocked`

2026-09-24 · claude/jev-popularity-comparison-t530yd · wrote docs/master 00-* plans + DORAX-BLUEPRINT; moved vendored skills to .claude/skills; added scripts/check.sh, .claude/hooks/stop.sh + graphify-guard.sh (not yet wired) · owner wires hooks + cleans .claude/settings.json (see 00-PROJECT-TREE.md §2) · agent may not edit its own permissions/guard hooks
2026-09-24 · claude/jev-popularity-comparison-t530yd · check.sh now tolerates the build.db prune flake (only when it is the sole error and the binary is fresh) — found on the Mac · owner: pull, re-run check.sh, then settings.json · —
2026-09-24 · mac-claude/claude/jev-popularity-comparison-t530yd · Phase 1 steps 1–8: settings.json split + deny rules + hooks wired; AGENTS.md single source; CI runs test.sh (target 26.1, AppIcon tracked, test.sh exit-code fix, 3 Xcode-26 compile fixes); PR template; milestone 1.0 + #9–#41; release-prep.sh + ship.sh from main, beta/stable channels, DMG out of git; brain issues → GitHub #42–#68, TODOs → #69–#74 · CI green, then protect main, open the PR · CI still red on Xcode 26.6 compile errors (run 5 pending)
2026-09-24 · mac-claude/claude/jev-popularity-comparison-t530yd · owner queue: 1 General Chat rename (7f18a2e), 2 Shift-only hotkeys rejected + test (0c78430), 3 §4 table checked (4dc384a), 4 as-built notes (d7773b4), 6 Global Context key on the ⌘⌘ row (6a59eeb); CI: test host skips launch (d1453d6) · open PR to general-chat-agent when CI green; then 5 (Selection in the Corner) on its own branch · item 5 waits for the PR merge
2026-09-24 · mac-claude/claude/jev-popularity-comparison-t530yd · CI red on PR #76: PluginScriptRunner hung when the host exhausted DispatchQueue.global (osascript/AE waits on CI); reads+wait moved to own threads, deadline bounds the drain; 20× plugin tests + full suite green · owner merges #76 once CI green, then step 2 (inventory check on the app) · —
2026-09-24 · mac-claude/claude/corner-fixes · owner's 4 Corner fixes (minimised app restore, folder preview fit+expand+pin, pins beside open field, Dock navigation via CornerNavigation) · PR #79; step 2 (inventory check) parked, blocked on driving DoraX (computer-use cannot see the app) · owner hand-tests swipes + visuals, merges #79
2026-09-24 · mac-claude/claude/parity-inventory-check · step 2 partial: 12 inventory rows checked on the app via System Events (C4-C7, C9, D4 ❌; D4 → runs a system command = safety bug; F2 ↑ in app scope opens list) · owner: fix D4 first? then 9 ❓ rows by hand · computer-use can't target DoraX (Spotlight off)
2026-09-25 · mac-claude/claude/corner-d4-up · PR #80 merged (one-shell dock + #79); #81: → steps in never runs (D4), ↑ reaches Global (F2), inventory branch folded in · found: first letter typed from resting strip lost (B5, from one-shell) · next: owner merges #81, then step 3 Selection
2026-09-25 · mac-claude/claude/corner-parity · step 3: Selection card lists+runs the Dock's Selection rows via SelectionActionProviding bridge (owner option B), captured selection only, AI rows → corner ask, Share left out, ✕ close; cold path proven (⌃S before ⌥⌥); debt #83 · next: owner merges, then step 4 keyboard rules · —
2026-09-25 · mac-claude/claude/corner-parity · #84: Selection open-speed/idle/hotkey fixes pushed; owner asked for answers+Share+file preview inside the Selection card and a full test matrix (Safari deep) · RESUME HERE: docs/superpowers/plans/2026-09-25-corner-selection-panel.md (owner answers Q1–Q3, then parts A–F) · —
2026-09-25 · mac-claude/claude/corner-parity · #84: parts A (answer in card), A2 (Computer Use gate on screen-taking rows), B (Share in card, native destinations + Share on answers) and a double-Esc fix pushed; full suite 1957/1957; next C (file preview); route 3 (typed "send to") awaits owner.
2026-09-25 · mac-claude/claude/corner-parity · #84: send-to (route 3) sent live to owner; row-by-row app check found+fixed 6 bugs (def7ef7); remaining checks listed in plan status; next C.
2026-09-25 · mac-claude/claude/corner-parity · #84: every Selection row and answer action checked on the app (TextEdit); follow-up caret fix; next C (file preview).
2026-09-25 · cloud/claude/jev-popularity-comparison-t530yd · added docs/master/00-NOW.md (work order + owner decisions, so nothing lives only in chat); inventory D13 ❓→❌ (Corner shows a tabs hint but loads no Safari tabs); daily brief now reads 00-NOW.md · Mac: finish Selection (task 1), then tasks 2–4 in 00-NOW.md · —
2026-09-25 · mac-claude/claude/corner-parity · #84 feature-complete: send-to confirmation, Settings Remove for Computer Use grants, inventory Corner-only section, D10 ✅, task 1 → Done in 00-NOW.md; owner tries then merges.
